# Supabase Skill (LifeOS)

- Use Supabase for auth, Postgres data, storage, and realtime where needed.
- Scope all user data by `user_id` and enforce RLS policies.
- Add migrations for schema changes; avoid ad-hoc production edits.
- Keep secrets in environment variables; never hardcode credentials.
- Return typed, actionable errors from data-access functions.
